import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#89BAD1',
      light: '#89BAD1',
      dark: '#89BAD1',
    },
    secondary: {
      main: '#F2C216',
      light: '#F2C216',
      dark: '#F2C216',
    },
  },
});
export default theme;
